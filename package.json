{
  "name": "artmentorai",
  "private": true,
  "scripts": {
    "test": "npm run test:backend && npm run test:frontend",
    "test:backend": "uv run pytest --cov-fail-under=0",
    "test:frontend": "npm --prefix frontend run test:ci"
  }
}
